# Redis Sentinel Configuration Example
# Copy these values to your .env file

# Redis Sentinel Mode (High Availability)
REDIS_SENTINEL_ENABLED=true
REDIS_SENTINEL_MASTER_NAME=mymaster
REDIS_SENTINELS=localhost:26379,localhost:26380,localhost:26381

# Redis Authentication
REDIS_PASSWORD=changeme

# Optional: Sentinel Authentication (if different from Redis password)
# REDIS_SENTINEL_PASSWORD=sentinel_password

# Note: When using Docker Compose, use container names instead of localhost:
# REDIS_SENTINELS=redis-sentinel-1:26379,redis-sentinel-2:26379,redis-sentinel-3:26379

